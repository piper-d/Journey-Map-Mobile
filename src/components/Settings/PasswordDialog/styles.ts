import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  error: {
    color: 'red',
    textAlign: 'center',
    lineHeight: 21,
    fontSize: 16,
    margin: 6,
    marginTop: 0,
  },
});
