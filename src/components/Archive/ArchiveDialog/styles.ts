import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    // width: 330,
    // height: 560,
  },
  deleteIcon: {
    position: 'absolute',
    right: 10,
    top: 10,
  },
  shareIcon: {
    position: 'absolute',
    right: 45,
    top: 10,
  },
});
